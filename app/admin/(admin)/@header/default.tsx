import { PageHeader } from "@/components/page-header";

export default async function DefaultDashboardHeader() {
  return (
    <PageHeader
      title={ "Dashboard" }
      description="Veja os resumos das informações contidas no painel administrativo">
      <div>

      </div>
    </PageHeader>
  )
}